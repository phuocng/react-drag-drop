import * as React from 'react';
import { TimePicker } from './TimePicker';
import './styles.css';

export default App = () => {
    return (
        <TimePicker initialHours={10} initialMinutes={20} />
    );
};
